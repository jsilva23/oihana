import styled from 'styled-components';
import Search from '../Search';

const HomeFeed = () => {
  return (
    <FeedBox>
      <Search />
    </FeedBox>
  );
};

const FeedBox = styled.aside`
 
`;

export default HomeFeed;
